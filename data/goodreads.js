// Populated from a Goodreads Library Export CSV via the in-app importer.
// Goodreads blocks automated shelf scraping, so this file starts empty and the
// Goodreads tab lets you load your export CSV (My Books -> Import/Export -> Export Library).
// Any imported library is also cached in your browser's localStorage.
window.GOODREADS = window.GOODREADS || [];
