services:
  library:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080" # put a TLS reverse proxy in front (see deploy/Caddyfile)
    env_file: .env
    environment:
      TRUST_PROXY: "true"
    volumes:
      - ./data:/data # library.sqlite + cached images
